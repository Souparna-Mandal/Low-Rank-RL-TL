% Written ONCE by experiments/src/export_atari100k_pgfplots.py and never overwritten:
% edit caption / label / placement freely (--rewrite-wrappers re-creates it).
% The picture itself is \input from the regenerated *_body.tex file.
\begin{figure}[t]
  \centering
  \input{\figtex/fig_model_free_bars_body}
  \caption{Atari-100k, 26 games, 100k environment steps: mean and median human-normalised score of published model-free agents (grey; the counts under the names are the trainable parameters of each paper's network) and of our EfficientRainbow baseline with and without the FHR loss (270 runs pooled, seeds 0--4). Published rows are quoted from one source each (EfficientZero Table~1; BBF Table~A.1).}
  \label{fig:atari100k-model-free}
\end{figure}
